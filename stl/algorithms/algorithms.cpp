#include <iostream>
#include <algorithm>

int main() {
    std::cout << "List of algorithms: " << "https://www.geeksforgeeks.org/algorithms-library-c-stl/" << std::endl;
    std::cout << "******************************\nManipulating algorithms\n";
    std::cout << "BASIC FORMAT:\tfunction(start, end)\n\n";
    std::cout << "sort(), reverse(), *max_element(), *min_element(), accumulate()\n"
                 "count(), find(), binary_search(), lower_bound(), upper_bound()\n"
                 "\n\n";

    std::cout << "******************************\nManipulating algorithms\n";
    std::cout << "BASIC FORMAT:\tfunction(start, end)\n\n";
    std::cout << "arr.erase(position), next_permutation(v.begin(), v.end()), \nprev_permutation(v.begin(), v.end()), distance(posStart, posLast))\n";

    return 0;
}